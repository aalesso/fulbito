<!-- Validation failure messages -->
<% if @slot.errors.any? %>
  <% @slot.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Slot #<%= @slot.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_slot/<%= @slot.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for date -->
      <div class="form-group">
        <label for="date" class="control-label">
          Date
        </label>

        <input type="text" id="date" name="date" placeholder="Date" class="form-control" value="<%= @slot.date %>">
      </div>

      <!-- Label and input for time -->
      <div class="form-group">
        <label for="time" class="control-label">
          Time
        </label>

        <input type="text" id="time" name="time" placeholder="Time" class="form-control" value="<%= @slot.time %>">
      </div>

      <!-- Label and input for courts_id -->
      <div class="form-group">
        <label for="courts_id" class="control-label">
          Courts
        </label>

        <input type="text" id="courts_id" name="courts_id" placeholder="Courts" class="form-control" value="<%= @slot.courts_id %>">
      </div>

      <!-- Label and input for users_id -->
      <div class="form-group">
        <label for="users_id" class="control-label">
          Users
        </label>

        <input type="text" id="users_id" name="users_id" placeholder="Users" class="form-control" value="<%= @slot.users_id %>">
      </div>

      <button class="btn btn-warning">
        Update Slot
      </button>
      or
      <a href="/slots/<%= @slot.id %>">Cancel</a>
    </form>
  </div>
</div>
